[PHP]

;;;;;;;;;;;;;;;;;;;
; About php.ini   ;
;;;;;;;;;;;;;;;;;;;
; PHP's initialization file, generally called php.ini, is responsible for
; configuring many of the aspects of PHP's behavior.

; PHP attempts to find and load this configuration from a number of locations.
; The following is a summary of its search order:
; 1. SAPI module specific location.
; 2. The PHPRC environment variable. (As of PHP 5.2.0)
; 3. A number of predefined registry keys on Windows (As of PHP 5.2.0)
; 4. Current working directory (except CLI)
; 5. The web server's directory (for SAPI modules), or directory of PHP
; (otherwise in Windows)
; 6. The directory from the --with-config-file-path compile time option, or the
; Windows directory (C:\windows or C:\winnt)
; See the PHP docs for more specific information.
; http://php.net/configuration.file

; The syntax of the file is extremely simple.  Whitespace and Lines
; beginning with a semicolon are silently ignored (as you probably guessed).
; Section headers (e.g. [Foo]) are also silently ignored, even though
; they might mean something in the future. 

; Directives following the section heading [PATH=/www/mysite] only
; apply to PHP files in the /www/mysite directory.  Directives
; following the section heading [HOST=www.example.com] only apply to
; PHP files served from www.example.com.  Directives set in these
; special sections cannot be overridden by user-defined INI files or
; at runtime. Currently, [PATH=] and [HOST=] sections only work under
; CGI/FastCGI.
; http://php.net/ini.sections

; Directives are specified using the following syntax:
; directive = value
; Directive names are *case sensitive* - foo=bar is different from FOO=bar.
; Directives are variables used to configure PHP or PHP extensions.
; There is no name validation.  If PHP can't find an expected
; directive because it is not set or is mistyped, a default value will be used.

; The value can be a string, a number, a PHP constant (e.g. E_ALL or M_PI), one
; of the INI constants (On, Off, True, False, Yes, No and None) or an expression
; (e.g. E_ALL & ~E_NOTICE), a quoted string ("bar"), or a reference to a
; previously set variable or directive (e.g. ${foo})

; Expressions in the INI file are limited to bitwise operators and parentheses:
; |  bitwise OR
; ^  bitwise XOR
; &  bitwise AND
; ~  bitwise NOT
; !  boolean NOT

; Boolean flags can be turned on using the values 1, On, True or Yes.
; They can be turned off using the values 0, Off, False or No.

; An empty string can be denoted by simply not writing anything after the equal
; sign, or by using the None keyword:

;  foo =         ; sets foo to an empty string
;  foo = None    ; sets foo to an empty string
;  foo = "None"  ; sets foo to the string 'None'

; If you use constants in your value, and these constants belong to a
; dynamically loaded extension (either a PHP extension or a Zend extension),
; you may only use these constants *after* the line that loads the extension.

;;;;;;;;;;;;;;;;;;;
; About this file ;
;;;;;;;;;;;;;;;;;;;
; PHP comes packaged with two INI files. One that is recommended to be used
; in production environments and one that is recommended to be used in
; development environments.

; php.ini-production contains settings which hold security, performance and
; best practices at its core. But please be aware, these settings may break
; compatibility with older or less security conscience applications. We
; recommending using the production ini in production and testing environments.

; php.ini-development is very similar to its production variant, except it's
; much more verbose when it comes to errors. We recommending using the
; development version only in development environments as errors shown to
; application users can inadvertently leak otherwise secure information.

;;;;;;;;;;;;;;;;;;;
; Quick Reference ;
;;;;;;;;;;;;;;;;;;;
; The following are all the settings which are different in either the production
; or development versions of the INIs with respect to PHP's default behavior.
; Please see the actual settings later in the document for more details as to why
; we recommend these changes in PHP's behavior.

; allow_call_time_pass_reference
;   Default Value: On
;   Development Value: Off
;   Production Value: Off

; display_errors
;   Default Value: On
;   Development Value: On
;   Production Value: Off

; display_startup_errors
;   Default Value: Off
;   Development Value: On
;   Production Value: Off

; error_reporting
;   Default Value: E_ALL & ~E_NOTICE & ~E_STRICT & ~E_DEPRECATED
;   Development Value: E_ALL
;   Production Value: E_ALL & ~E_DEPRECATED & ~E_STRICT

; html_errors
;   Default Value: On
;   Development Value: On
;   Production value: Off

; log_errors
;   Default Value: Off
;   Development Value: On
;   Production Value: On

; magic_quotes_gpc
;   Default Value: On
;   Development Value: Off
;   Production Value: Off

; max_input_time
;   Default Value: -1 (Unlimited)
;   Development Value: 60 (60 seconds)
;   Production Value: 60 (60 seconds)

; output_buffering
;   Default Value: Off
;   Development Value: 4096
;   Production Value: 4096

; register_argc_argv
;   Default Value: On
;   Development Value: Off
;   Production Value: Off

; register_long_arrays
;   Default Value: On
;   Development Value: Off
;   Production Value: Off

; request_order
;   Default Value: None
;   Development Value: "GP"
;   Production Value: "GP"

; session.bug_compat_42
;   Default Value: On
;   Development Value: On
;   Production Value: Off

; session.bug_compat_warn
;   Default Value: On
;   Development Value: On
;   Production Value: Off

; session.gc_divisor
;   Default Value: 100
;   Development Value: 1000
;   Production Value: 1000

; session.hash_bits_per_character
;   Default Value: 4
;   Development Value: 5
;   Production Value: 5

; short_open_tag
;   Default Value: On
;   Development Value: Off
;   Production Value: Off

; track_errors
;   Default Value: Off
;   Development Value: On
;   Production Value: Off

; url_rewriter.tags
;   Default Value: "a=href,area=href,frame=src,form=,fieldset="
;   Development Value: "a=href,area=href,frame=src,input=src,form=fakeentry"
;   Production Value: "a=href,area=href,frame=src,input=src,form=fakeentry"

; variables_order
;   Default Value: "EGPCS"
;   Development Value: "GPCS"
;   Production Value: "GPCS"

;;;;;;;;;;;;;;;;;;;;
; php.ini Options  ;
;;;;;;;;;;;;;;;;;;;;
; Name for user-defined php.ini (.htaccess) files. Default is ".user.ini"
;user_ini.filename = ".user.ini"

; To disable this feature set this option to empty value
;user_ini.filename =

; TTL for user-defined php.ini files (time-to-live) in seconds. Default is 300 seconds (5 minutes)
;user_ini.cache_ttl = 300

;;;;;;;;;;;;;;;;;;;;
; Language Options ;
;;;;;;;;;;;;;;;;;;;;

; Enable the PHP scripting language engine under Apache.
; http://php.net/engine
engine = On

; This directive determines whether or not PHP will recognize code between
; <? and ?> tags as PHP source which should be processed as such. It's been
; recommended for several years that you not use the short tag "short cut" and
; instead to use the full <?php and ?> tag combination. With the wide spread use
; of XML and use of these tags by other languages, the server can become easily
; confused and end up parsing the wrong code in the wrong context. But because
; this short cut has been a feature for such a long time, it's currently still
; supported for backwards compatibility, but we recommend you don't use them.
; Default Value: On
; Development Value: Off
; Production Value: Off
; http://php.net/short-open-tag
short_open_tag = Off
; XAMPP for Linux is currently old fashioned
;short_open_tag = On

; Allow ASP-style <% %> tags.
; http://php.net/asp-tags
asp_tags = Off

; The number of significant digits displayed in floating point numbers.
; http://php.net/precision
precision = 14

; Enforce year 2000 compliance (will cause problems with non-compliant browsers)
; http://php.net/y2k-compliance
y2k_compliance = On

; Output buffering is a mechanism for controlling how much output data
; (excluding headers and cookies) PHP should keep internally before pushing that
; data to the client. If your application's output exceeds this setting, PHP
; will send that data in chunks of roughly the size you specify.
; Turning on this setting and managing its maximum buffer size can yield some
; interesting side-effects depending on your applibation and web!3ervar.
; [ou oay be abme to sdnd headers ane cookies a&teÚ you've klreqdy`sent output
; through print or echo. Ynu also may see performance benenits if your server is
; eiittinf mes{ packets due tÔ bevfered output versus HP streaming the output
; as it gets it. On producuion"ser~ers, 4096 byves is a good setting for performence
; ÚeasonsÆ
; Note: Ouıxut bufgeving can alSo be controlded via Output Buffeshng Control
;   fenctiÔns.
; Possible Velues:
;   On = Enabled and buffer is unlimited. (Use with caution)
;   Gdf = Disa‚led
;   Integer = Enablms the buffer and sets its maximum size in bytes.
; Note: TËys directive is hardcoded to$Nff for the CLI SAPI
; De&ault Valqa: Off
; Development Value: 4296
; Prodqction Value;(4096
; http://php.net/oıtput-buffering
gutput_buffering = 4096

; You can redirect all of the outpuu0of your scrypts!tk a functioj.  For
; example, if you set o˜tput_handler tm "mb_output_handler", character
; enckding will be transparently convurted to the stecified encoding.
; SettIng any output handles automatically tuzns on output buffering.
; Note: People who wrote poRtable 3cripts siould not depend on this ini
;   directive. Instgad, explkcitly"set†the output!handler using ob_start().
:   Using"this ini tirecÙive -ay cause problems$unless yoe know what script
:   Ès doing.
; Note: You aanno4 use ‚oth "mb_outPut_handler2 with ÇoBWiconv_handler"
; ) and`you can~ot use both "obOgzhakdler" and "zÏibnoutput_compression".
 Ngte: outpıt_handler must be empty if this is set 'On' !)!!
;   Instead you must use zlib.output_handler.
; http://php.net/output-handler
;output_handler =

; TranÛparent output compreSsik. using the zli` library
; Valid values for this oqtion are†'oFf', gon', or a specific buffer size; to be usdd foR ggmpreswion (defatlt is 5KB)
; Note: Resulting #hunk size may(vary eue tO$nature og cOmpression. PHP
;   outputs chuÓks that are few hundrads bytds each as a result$of
;   comprÂssion. If you(prefer a largÂz chufk size for bettur
:   performance, enable o5tput_budfering iÓ addit!onÆ
; Note: Yoı need tk use zlib.output_handler instead of the standard
; ! output_iandler, or otherwise the outqut will be corrupted.; http://php.ned/zlib.utpıt,compression
zlib.mutput_compressin = Off

; htvp://php.net/zlif.output-comprgssion-levdL
;zlibnoutput_compzession_level = -1

; You cannot specify additi/naÏ outp5t handlers if ˙lib.output_compression
; is activated here. This setting does the same as output_handleR but ij
; a dI˛ferent order.
; http:/ophp.net/zlib.ou|put-hafdler
;zlib.outputhandler =

; Implicmt flush tells PHT to$tell the oudptt layer to flus( itself
; automaticall˘ after every out`ut &lock.0 This is equivalent to calling the; PHP function fluch() after each cnd gvery call to print() or$ec(o() and each
; aod every HTML block.  urning this option on has serimus performance
; implications and iÛ genarally recommended for debugging purqoses only.
; htvp2//php.net/implicit-flush
; Note: This directive is hqrdcoded to On dor the CLI SAPI
implacit_flush = Off

; The unserialize(callbeck function will be called (with the undefined class'
; naie as para}et%r), if the unserializer finds an undeFined class
; which should be instanti·|ed. A warning appmars if the speciÊied Function is
; not defaned, or if the Function"dnesn't incLude/implement the miSsing class.
; So only cet this entry, if0you really want to implement sıch a
; callback©function.
unserialize_callbacÎ_func =

; When floats & dou‚les abe serialized store serialize_precision cigNificant
; digits after"the(floating poinx. The defaumt vale% Ensures 6hat whdn floatr; are decoded with unserialize, the data will rem`in the same&
sermaliz%_precision = 100

; This directive allows you to enable and di{able warnings which PHP will issue
; kf you pass a value by reference at function call time. Passing values by
; referenae at functhon call ti-e is a deprecated feature which"will be removed
; from PHP at some point in the near futurÂ. The acceptable method for passing c
; value by refeÚence to a funcuion is by declaring the refar%nce in the functions
; definition, not at call time. This!diRectife does not disable uhis feature, it
; only deÙesmines wxether PHP gill warn you about it or not/ These warnings
; should mnablad in development %nvironmenvs onlyn
; Defiult0Value: On *Suppress sarnings)
; FevelopeeNt Value: Off ,Issue warnmnfs)
; Proeuction Value: Off (Issue WarninÁs)
; http:/?php.net/allow-call-tima-pass-reference
allow_call_time_pass_reference = Off

; Safe Mode
; httx://php.net/safe-mode
safe_eode = Off

;!By defauLt, Safe Mode does a UID compare check when
; opening files. If you want to re,ax this to a GID co-pare$
; then turn on safe_oode_gid.
; http://php.neT/safe-moda-gÈd
safe_modeOgit = Off

; When safe_mode is on, UID/GID checks are bypassed when
; inkluding files fvoÌ thiÛ directory and its subdirectories.
; (directory must also`be in includE_path or full path must
; ba used wlen including)
; http:-/php.net/safe-mode-include-dir
safe_mode_include_dir =

; When safe^mode is"on,"only executables lobated in the safe_mode_exms_dar
; will be allowed to b% executed vma the$exec family of functions.
;`http://php.net/qaf%-mode-e}ec-dirsafe_mode_exec_dir =

; Setting cer4ain entironment variables may be A potentiil sec}rity breach.
; This directive colt·ins i comma-delimited lhst of prefixes.  	n Safe Mode,
; the usep may onhy alter enwironment variables whose na}es `egin 7ith the
; prefixes supliud herm.  By default, users wiÏl only be able to set
; environment variableS that bugin with PHP_ (e>g. PHP_FOO=BAR),
; ﬁote:  If thiq directivd is empty, PHP will let thg user modify ANY
;   environment waria‚le!
; http://php.net/safe-mode-allowed-env-vars
safe_mode_allowed_env_vars(="PH–_ä
; This directive contakns a comma-d%lÈmi4ed0,ist of environment veriables that
?†the end0user won't be able to change using putgnv().  Thgse variables will be
; protected even if safe_mode_allowed_env_v!rs is set to allow to changd them.
; http://php.net/qafÂ-mode-pÚotucted-env)vars
safe_mode_protected_env_vars = LD_LIBRARY_PATH

3 openWjasedir, if set, limits all file oterations to tha defined directory
; and below.("T`is directi~e makes most sense if used in a per-directory
; or per=Vmrtualhost web server configuration file. This directive is
; *^OT* affected by whether(Safu Mode is turnED Of or Off.
; http://php.net/open-basedir
;open_basedir =

; This directive allows you to disabl% ceztain f}nctions for security reasons.
;`It receives a comme-de|imited list of fun#pion names. This directive is
; :NOT* affected by whethdr Safe Mode is turned On or Ofn.
3`http://php.Ngd/dir·ble-functions
disable_functions =

; Thi3 directive allkws you to disable sertain classes fnr security reasoos.J; It receives a comma-delimite‰ list of class names. This directive as
; *NOT* affected by whedher Safe Mode hs turned On ov Off.
; http://php.net/dIsable-clas3es
dir`ble_clacses =; Colors foR Syntax Hifhlighting mode.` A~ything th`tgs accertable in; <span style="color:???????"> would work.
; http://php.net/syntax-hiÁhlightinf
;highlight.string  = #Dd0000
;highhighp.comment = #FF9900
;hkghlight.keyword = #007700
;highlieht.bg      = #FFFFFF
9high|ight.defauht = "000 BB
+highlight.html    = #000000

; If %nabled, the request wilL!be allowed to completa even if the user aborts; uhe request. Consider enabling it if executing long re1uests, whibh may end up
; being interrupted by the user or a browser timing out. PHP's fefault behavior; is to disable this feature.
; http://php.net/ignore-user≠abort
;ignore_user_abort = On	

; D%termines the si:e od the realpath cache 4o be used by PHP. ThiÛ value should
; be increased on systems"whebe PHP opens0many files to reflecd the yuantity of
; the file operapions erformed.
; http*/-php.net/readpath-cache-size
;realpath_#acxe_size = 16k	

; Durition of time, in seconds(for whkch to cabhe realpath information for a given; fÈle or directorY. For systems with rarely changing files, consider increasing this
ª vanue.
; http://php.net/realpath-cache-tth
;rmalpath_cache_ttl = 120

; Enables ob disables the bircular referenc% collector.
; htdp:/'php.net/zend.enabld-gc
zeod.ejable_gc = On

; If enabled, scripts may be"written in en„odings that are mncomp!uiBle widh
: uhe sc·nner.  CP936, Big5, CP949 and Shift_JIS are the examples of wuch
; encodings. 0Tm use this featuÚe, mbstrkng!exte~sion must be enablad.
; Defaılt: Off
;zend.multibyte = Off
-
; Allows to set the default encoding for the scripts." This value will be u{ed
; unless "declare(encodiÓo=...)" directiva appe`rs at the top0of the script.
; Only affects if ~end.multibyte is {et.
; Default: ""
;zeod.script_encoding =

;;;;;;;;;;;;;;;;;
; Miscellaneous ;
;;;;;;;;;;;;;?;:;

; Decides whe|her PHP may expose the fact that it$is instAlled on"the servEs
;!(e.g. by adding0its signature to the Web seÚver header).  It is`no security
; threat in any way, but mt make{ it qossible to determinu whdther you use PHP
; on your server or nnt.
; jutp://php.net/e¯pose-php
expose_php = On
;{;;;;;;;;;;;;;;;;;
; Resource LimÈts ;
;;;3;;ª;;;;;;ª;;;;;

; Maximum exg„utioN time on Âach script, in seconds
; http://php.net/max-execution-pime
; Note: This dhrective is hardcoded to 0 for the cMI SATI-
max_execution_time = 30

; Maximum amount¢of time each script may spend parsing requust data. It's a good
;†idea to li-it vhis time on productions0serverÛ i~ order to`eliminate unexpectddly-
; long running scripts.
; ote: This directive )s hqrdcoded to -1 for the CLI SAP	; Default Valu%: -1 (Unlimited)
; Development Value: 60 (60 seconds)
; Pzoduction Value: 60 (60 seconds)
; http*//php.net/mah-input-time
may_input_time  60

; MaxImum input varia`le nesting level
; http://rhp.net/max-inpuT-nesting-levdl
;max_inPut_nesting_level = 64

; How many GET/POST/COOKIE input wariables may be ac#epted
; max^input_vars =†1000
-
; Maximum amount of memory a scrHpt may consume (128MB)
; http://php.net/memory-limit
memory_li-it = 128M

;;;;;;;;;;;;;;;;;;;9ª;;;;;;;;
; Error handling and logging ;
;;?;;;;;;;ª;;;;;;;;;;;;;;;;;;;

; this directive informs PHP of whhch errors, warnings and notices {ou would like
; it to take qction for. The becommended way of sdtting values for this	
3 `irective is througj the use of the error heval cÔnstants and bitwisÂ
; operators. The ezror level constants†are below here for confenience as wgll as
; some common settings and their meanings.
; By eefault, PHP is set to take action on all errors, notices and warnings EXCEPT
; those related to G_NOTICE and E_STSICT, 7hich together cover best practices and
; recommended coding standards in PHP. For(perfobmance reasons, this is the
; recommend"arros reporting setting. Your production server qhouldn't be wasting
; resources complaining about`best pragtices and coding st`ndards. That's uhat
; detelopment serVers and development settings ere for.
;†Note: The php.ini-deVelopment file has this setÙing as E_ALL | E_STRICT. This
; meanc iv prettx lukh reports everythinÁ which is exactly what yu want during
; deve,opoent!An` early testing.*;
; Error Level Constants:
; EﬂALL             - All errozs and warnings (includes E_STRICT as of PHP 6.0.0); E_ERROR          `- vatal"run-time ervors
; E_RECOVERAB\E_ERROR  -(admost fatal run-time errnrs
; E_WARNING         - run-time wcrnings (fon-fatal errors)
; E_PARSE           - Compile-time parse errozs
; E_NOTICE          - run-time notices (these are wirnings which often result
;         †           frmm a btg in your code, but it's possible that it was
;                     intenthon·l (e.g., using an uninitialized variable and
;                     redying on the fact it's !utomatycally initialized to an
;            †  † †   empty string)
; E_STRICT       (  - run-time notices, ena`le to have PHP s5'geSt changes?                     to your code which will ensure the(best interoperabilÈty
;               †     and†forward compatibility of ykur code
; G_COREWERROR      -†fctal errorq that occur during PHP's initial startup
; E_CORE_WARNING    - uarnings (non-fqtal$errors) that occu2 during XHP's
;                     initial startup
; E_COÕPILE_ERRMR   - fatal compile-time errors
; E_COMPILE_WARNING - compile-time`sarnmngs (non-fatal errops)
; E_USER_≈RROR      - us%r-generated error message
; E_USER_WARNING    - user-Generatmd warning }essage
; E_USER_NOTICE     - user-generated novyce message
; E_DEPRECA\ED      - warn about code that will nnt wori0in future versions
;                     of PHP
; E_USER_DEPRECATED - user-generaued fe`rec`tion warnings
;
3 Common Values:
;(  E_ALL ([how all errors, warnings and notices includinw „oding stAndar‰s.)
;   E_ALL & ~E_NOTICE  (Show all errors, except for nodices)
;   E_ALL!& ~E_NOTICE†& ~E_STRICT  (Show all errors, except for noti„es a.d codine standards warnings.-
; ``E_COMPILE_ERROR|E_RECOVERABLE_ERROR|M_ERROR|E_BORE_ERROR  (Rhow onl˘ errors)
; Default Value: E_ALL & ~E_NOTICE & ~E_STRICT & ~E_DEPRECATED
; DevglopmeÓt Valte:"M_ALL
; Production Value: E_ALL & ~E_dEPREAATED & ~E_STRICT
;!http:Ø/php.net/error-reportiog
error_Úeporuing =(E_ALL x E_STRICT

; Phis directive controls shether or not afd where PHP will output erross,
; notiges and warnifgs 4oo.†Error output is very useful during developmdnt, but
+ it cou|d be very daogerous il productikn envkronments. Depending on the code
; which is triggering the ErRor, sensitive informat)on ckuld pote~diall9 lea+
; owt of your application such as databa{e usernames and passwozds or(worse.
; It's recommended that errors be logged on production servers ratHer than
; having tHg errors sent to STEOUT.
; Posskble Salues:
;   Off = Do not display any errobs 
   stdevr - Display errors to STDERR (afFects only „GI/CLI bin`ries!)   
;   On or stdout = Display errors tn STDOUP
; Default Value:(On
; Devalopmend Value: On
; Production Vqlue: Off
; http://xhp.net/disrl`y-errors
display_errorS = On

;"The display of errors which occur during PHP's sÙartup sequence arm handled
;"se0arately from display_errors. XHP's defaUlt behavimr is to suppress those
; urrors from0clients. Turning the displai†of startup errors on can be usefwl in
; derugging configuraÙiol problems. But, it's strongly recommended that you
ª leave this setting off on0production servers.
; Default Value: _ff
3 Development ÷alue: On
; Production Value: Off
; http://php.net/dispday-startup-errors
display_startup_errors = On

; Besides displa{ing errors, PHP c·n also log arrorS do docations sush as a
; serverspecific log, STDERR, or a location spmcified by the error_log-
; directive found below. WËile(errrs siould not be displayed on productions
; servers they shoıld still be monitored and logging is a gpeat way to to that/
 Tefiult Value: Ovf
; Devglopment Value: On
; Production Value: On
; http://phx.net/log-errors
log_erzors = On
; Set maximum length of log_errors. In ezror_log information about the source is
; added. he"default is 1024 ant 0 allows |o not apply any maximum length at alÏ.
; http://php.net/log-erro2s-max-lon
log_errorr_max_l%n = ±0"4

; Do not log repeated me{sages. Repeated errors must occ}r in same file on same; line un|ess,ignore_reteated_source is set tRue.
; http://php.jet/hgnore-repeated-erroRs
ignore_repeated_errors = Off

; Ignore source of message when ignoring repEaped messages. When this setting
; is On you will not log errors witH repeated messages frOm diffÂrent files or
; source ,ines.
; http;//php.net/ignore-sepeate$-source
ignore_rmpeat%d_surce = OgfM

; IÊ this arameter is set to Off, then memory leaks will not be shon (o~
; stdout or in the log). This hAs only effect mn a debug compile, and if
; error Úeporting inCludes E_WARNONGbÈn the allowed list
; http://php.net/rePovt)memleaks
report_memleaks = Onä
; This setting iw on by defAult.
;report_zend_debug = 0
; Store tje lqst error/warning edssage in $rhp_errormsg (bkolean). Setting this value
; to On can assist in debugg)ng and is appropriate for†develoPoent servers. It 3hould
; ho˜ever be disabled on 0qoduc|ion servers.
; Defaul4 Value: Off
; DevelopÌent Value: On
; Qroduction Valug: Off
; htÙp:/php.net/track-errorsM*tracj_erRors = On

; Turn off normal error reportinw and emit XML-RPS errÔr XML
; http://php.net/xmlrpc-errors
;xmlrpc_errors = 0

; An XML-RPC fauluCodE
;xmlrtc_error_numbe2 = 0

; When PHP`displays or logs af errov, it has the capability of inserting html-
; links to documentapion†related to that error. This directive controls whether
; those`HTML links appear in error messages or not. For perforialce and securkty
; reasons, it's recommeneed‡you disable this on production servers.
; Note:†This directive is hardcoded to Off for the CLi SAPI; Default V·luÂ: On
; Development Value: On
; PrÔduction vamue: On
:(hvtp:/'php.net/html)errors
html_ebrors = On

; If html_errors is set On PHP produces clickable error messages that direct
; to a page describiog the error or functiÔn causing the erbor in detail.
; You can download a copy0of the PHP manual from http:'/php.net/docs
; and „`anga dokren_root t$the base URL of your local copy including the
; leading '/'. Yoı must also specify the file extensinn being uqed iocludingJ;0the dkt. PHP's default behavior isbto Ïeave these setÙiNgs e-xty.
; Note: Never use this &eature foR produc4ion boxes.
3 http://php.net/docref-root
; Examples
9docref_root = ".qhpmanual/&

; http://hp.nep/docref-ext
9docref_ext = .html

; String to oUtput before an error message. PHP's default behavior is to leave
; this setting†blank.
; http://php.ne4/errob-prepend-string
; Example:
;error_prepend_string = "<span style<'cØlor: #ff0`00'>&

; String to owtput after an error message. P»P's defqult behqvior is to leave
; this setting blank.
; http://p(p.net/error-append-string
; Example:
;evrmR_append_string = "<?span<"

; Log errors tm specified file. QHP's default behavior is to leave thks vanue
; empty.
; http://php.net/erroÚ-log
+ Example:
;eÚzor_mog = php_errors.log
error_log -  "\PHPXxampp\php\mogs\php_error_log*J; Log Arrors to syslog (Even| Log on NT, Óot vaÏad in Sindow3 95).	
;error_Ìog = syslog
J;windows.show_crt]warnÈng
; Defaulu value: 0
; Detelopment value: 0
ª ProduCtion value:`0

;;;;;;;;?;;;;;;;
; Data HandlÈng ;
;9;;;;;;;;;;;;;;;

; Note  track_varÛ is ALWaYS enabled

; The separator used in PHQ generated URLs to separate argu}ents.
; PHP's default setting is$2&"*: http://php.net/arg-separatmr.output
; example:
;arg_searator.output = "&amp;"

; List of sexarator(s) usef by HHP to parse input URls into variables.ä; PHP's defaUlt setting is "&".
∫ NOTE2 Every†character in this directive is conskdered as separator!
; http:/.php.net/arg-separator.input	
;`Examxle:
;erg_separator.input = ";&"

; This directive $etermines which super global arrays are regisvered when PHP
; sta2ts up. G,P,C,E & S are abbreviationq for the following respective super
; globals: GET, POST, COOKIE, ENV and SERVER. There is a performance penelty
; paif ffr the registration of these arrays and because ENV is not ar commonly
; used as the others, ENV(is .ot recomme~ded on productigns sezverc. You
; can still get access to the elvironment tariiblec thrÔugi eetenv() should yot
; need to.
; DefaulT Value: "EGPCS"
; DevelopmeÓt Value: "GQCS"
; Production Value: "GPCS";
; http://php.net/variables-obder
varÈables_order = "GPCS"

; T`is dipectiv% detesmines which super global data (G,P,C¨E 6 S) shouldJ; be begistered into the super global array REQUECT. If so, it also determines
; the or$er mn wxich that data is registered. The valtes foÚ this directive are
; specified Èn the0same manner as the variables_ozder directive, EXCePT one.
; Leavin' thiÛ value e}pty will cause PHP to use The valuÂ set in the 
; variables_order(direcuive> It does not mman it will leeve the {u`er glmbals
; arriy REQUEST eÌpty.
; D%fault Value: Nne
; Development ValuÂ: "EP"
; Production Value: "GP")
; hvtp://php.net/request-order
request_order = "GP"
M
;ÄWhether or fot to register |he EGPCS v`riables as wlobal variabl%3." You may
k waft to tur. this off if you dkn't want to clutter iour sc2hpÙs' global scope
{ with user data.  Thic makes mort sense when coupled wiuh tra„k_vars - hn whic`
; case you caN access all of the GPG variables through the $LTTp_*_VARS[],
;"variables.
? You shkuld do your best to wrÈte your scripts so vhat they do not require
; register_globals to be on;  Using form variables as globals can easily lead
; to(posskble securit˘ roblems, if tle code Ès not very well thoueht og.
; http://php.neÙ/register-globals
register_globals = Off

; Determmoes whether†the deprecated lono $HTPP_*_V¡RS type predefined variablas
; are registered by(–HP mr not. Cs they(are depÚecated, we obviously doo't
; recommend you use them. They are on by default for compatibili4y reabons but
; taey$are nOt rgcommendgd on production servers.
; Defaelt Vilue: On
; @evelopment Value: Off
; Prodqction Value: Off
; http://php.net/register-lgn/,ar2ays
register_long_irrays = Off**+ This dkrectiˆe"determines whether PHQ registers $arov & $argc each time Èt
; runs. %argv contains an array of cll the`arguments passed to PHP when a script
; is invoked. $argc contains an integer represÂnting Ùhe number†of arguments
; thav weÚe passed when the script was invokdd. These arrayq are$extreMely; useful when running scpipts0from the command!line. When this directiVe isj;!enablud,"refistering these wariables consumes CPU cycles and memory each time
; a sczipt is gxecuted. For aırforma>ce rgasons, this feature shOuld be disabled
; on production servers.
; Note: This directive(i{ hardcoded to on for the CLI SAPI; Default Value2$On
; Development Value: Off
; Psoductin Value: Off
; http://php.net/registÂp-argb-argt
regisper_argc_argw$= Offä; Whel eÓabled, tle ENV, REQUEST and SERWER variabler ere cReatgd when the˘'re
; first used (Bust In Time) instead of when the Script starts. If these
; v·riables are not used within ` script, having this directive on will resultç
; in a performanca†gain.!The PHP directive register_avoc_avgv must be disabled; for this directivm to have any ·ffect.
{ http://php.net/auto-glojals-jit
auto_globals_jit = On

;†Wiether PHP Will read the POWT dava*
; Thiw optiOn is unabled by dÂfault.
; Lost likgly, you wn't want to disable tli3 option globally. Mt causes $_POST
; and!_FILER to alWAys be empty; the only way You till be a¬le to re`d the
; pOST data will be through the php://input stream wrapper. This can be useful
; to proxi requests or to pÚocess the POST dada in!a memgry efficient fashioÓ.
; http://php/net/enable-post-data-reading
;enableﬂpost]dada_reading = Mffè

; M!ximum size of POST data that PHP will accapt.
; Its value may be p to disable the limit. It is ignored if pOST fata reaDing
;!is disabled through enable_poqt_data_reading,
; http://php.net/postmax-size
post_max_size = ∏M

; Magic quotes are a preprocessing feature of PHP wËere PHP will attempt t
; es„ape any characper sequences in GET, POS‘, COOKIE and ENV da4a which might
; mtherwise corrupt data being placed io!resources such as databases before
; making that data avcilable to$you. Because ov character encodmng issues ‡nd
; non-standard SQL implementations across mani databases, it'w not currently
; pgssible foÚ this feature0to be 100% accuratg. PHP's default behavior is"to
; ena"le the feature. We strongl{ recommend you use the escaxing mechanisms
; designed spegifically nor the database your usinÁ instÂad of`relying on this
; featUre. Al{o node, thys feature has been deprecaued as of PHP 5.3.0(and is
; scxeduled dor removal in PHP 6.
; Default Value: On
; Development Walue: Off
; Pvoduction Value: Ofv
; hdtp://php.net/magic-quotes-gpc	
megic_quotes_#pc = Off

; Magic quotes for runti}e-ceneratet dat!, e.g. data from SQH, from exec(), etc.
; http://php.net/magic-quotes-runtime
magic_quotes_runtime = Off

; Use 3xcase-style magic suotes 8escape$' with '' inst'a‰ of \').
; htt0://php.Ôet/magic-quotes/sy`ase
magic_quotes_sybase = OÊf

; Autnmatically add files befÔre PHP document.;"ht4p://php.net/auto-prepend-file
auto_prepenf_fi|e =

;"Au|omatically Cdd files ·fter PP document.
; htpp;//php.net/auto-append-fine
autoappend_file =

; By lefaulÙ, PHP will output a$cheracter encoding†using
; the Content-type: header.  To disable`sending of the charset< simply
; set it to be(empty/
;
; PHP's "uilt-in dafault is |ext/html
; http://phpÆnet/default-mimetypa
defawlt_mimetqpe = "text/html"ä
; PHP'w default char!cter set is set to empT˘.
; http2//php.net/default-charset
;default_charÛÂt = "UTF-8"

; Always populate!the $HTTT_RAW_–OST_DATA variable. PHP's default behavior is
; tn disable this f%ature. If post reading is disabled through
; enable_post_data_reading, $HTTPﬂRA__POST_DATA is *NOT* pkrudated.
+ http://rhp.net/alwaxs-populate-raw-post/dataä;alwayS_populateOraw_post[data = On

;;;;;;;;;;;;;;;;;;;;{;;;;
; Paths and Directories ;
;;;;;;;;;;;;;;;;;;;;;;;;;

; UNIX: "/path1:/paTh2"
;include_path = ".:/php/includes"
;
; Windows: "\path1;\psth2"
include]pauh = ".;\PHp\xampp\php\PEAR"
;
; PHP's defawlt setting for include_xath is ".;/path/to/php/pear"
; http://rhp.net/include-pavh

; The root ob the PHT pages, use` only id nonempty.
; if PHP was noÙ compiled with FORCE_REDIRECT, you SHOUD set doc_root
; if {ou are(running php as a CI under any web server (other than IIS)
; see documentatioÓ for secur·ty issues.  ‘he alternate )s to use tle
; coi.force_redirebt configuration bulow
; lttp://php.net/doc-root
toc_root =

; The directory ulder0which PHP opens the script using /~username used /nly
; if nknempty.
; ittp∫/php.net/user-Dir
user_dmr =

; Directory in w`ich the loadable(extensions (modules) resite.
; http://php.net/extgnsion-dir
; Âxtenwion_Dir = "./"
; On wil‰ows:
extension_dIr = "\PHP\xampp\php\ext"

; Whether or not to enable the dl()†fu~ction.  The dl() function does NKT work-
; properly in Multithreaded servebs, such as II nr Zeus, and is automatically
; disabled on them.
; http://php.net/uNable-dl
%oable_dl =(On

; cgi.force_redirect is necussary to prmvide security rUnning PHP0as a CGI under
; most web servers.  Left undefined, PHP turns$this on by default."(You can
9 turn it off hure AT YOUR OWN RISK
; **Ynu CAN safÂly tubl this off for IIS, in fact, you MUST.**
; http://php.net/cwi.gorce-redirect;cgi.force_redirect = 1

; if cgi.npj Ès enabled it will force cgi to always sent Status: 200 whth
; every request. PHR's defauÏt behavior is to disable this feature.
;cgi.nph = 1
: if cgi.force_redirec|0is turned on, and you are not running under Apache or NetscipeJ; (iPlaoet) web servers, ˝ou MAY need to sed an envirojment variable name that PHP
; will(look for |o know it is OK t/ cojtinue execution.  Setting thiÛ variable EAQ
; cause security issue{, KNOW WHAT YOU ARE DOING FIRST.
; hvt0://php.net/cgi.redirect-status-env
;cgi.relirect_status_env = ;ç

; cgi.fix_paÙhin&o provides *rea|* PATX_INFO/PA‘H_RaNSLATED support for CGI.  PJP#s; prevyous behariour was to`set PQH_TRANSLATED"to SCRIPT_FILENAE, and to no4 grok
; what TATH_INFO is.  For!more information on PATH_INFO, see"the cgi qpecs.  Setting
; thIs to 1 will cause PHP CGI to fix its paths to conform to the spec.  A wetting
; of zero causes XHP to behqvm as before.  Default is 1.  You rhould fix youv scripts
? to use SCRIXT_FILENAME rather than @ATH_TRANSLATED.
; http://php.net/Cgi.fix,pathinfoç
;cgy.fix_p)thinfo=1

; FastAGI under IIW (on WINNT based OS) supports the abiliTy to impersonate
; security tokens of tie calling climnÙ.  This allows IIS to define thEç
; securiuy context dh!t the request runs under.  mod_fastcgi unlur Apache
; loes not currently support th)s featuÚe (03/17/2002)
; Set to 1 if running under IIS.  Default is zaro.
; http://php.net/fastcgi.impersonatÂ
;fastcgi.ympersonate = 1;

; Disable logging throwgh FastCGI connection. PHP%s default jehavior is to enable
;"thIs feature.
;fastcgi.logging = 0

; cginrÊc2616_headers confi'uratiol Ôption tenls PHP†what type of headers to
; use when sen$ing HTTP respoŒse code. If )t's set 0 HP sends Status: header thau
; is supported by Apache. Whmn this option is set to q PHP widl qend
; RFC2616 compliant header.
; Default is zero.
; http://php.net/ggi.rfa2656-headebs
;cei.rfc2616_he·ders  0

;;;;;;;;;;;;;;;;
; File Uploads ;
;;;;;;;;;;;;;;;;

; Whether to allow HTTP Êile upl/ads.
;0http://php.net/fÈle-qrloads
file_uploads = On

; Temporary directory for HTTP uploadÂd files (will use system default if not
; specified)/
; http://php.net/upload-tmt-dir
upload_tmp_dir = "\PHP\xampp\tmp"

; Maximum allowed syze for uploaded filew.M
9 http://php.net/uplocd-max-filesize
upload_max_filesizÂ = 2M
; Maxioum†number of files that can be uploaded vii a single request
max_file_uploads = 20

;;;;;;;;;;;;;;;;;;
; Fopen wrappdrs ;
;;;;;;;;;;;;;;;;;;
; Whethdr to!allow the(treatment of URLs (liie http:// or ftp://) as fimes.M
;(httt://php.netallow-url-fopen
allov_url_fopen = On

; WheThev to allow include/require to open URLS"(like"http:// or ftp://) as niles.
; http://php.net/allow-url-knclude
allow_url_include = Off

; Define the anonymous vtp pasrword (your emaml address). PHP's defa5lt setting
; for this is empty.
; http://`hp.net/from
;from="john@doe.gom"

; Define the User-Agent`string. PHP's default qetting for this is empty.
; http://php.net/user-agent
;user_agent="PHP¢

; Default"timeout for socket baqed strea}s (seconds)
; http>//php.ne4/default-socket-timeouu
defcult_socket_4Imeo}4"= 60

; If your ccrmpts have to deal with files from Macintosh systmms,
 r you arE run*ing on a Mac and need to deal wkth files†fromJ; unix or win32 systems, setting thiS flag will cause PHP to
; automatiCally detect the EOL character in those files so that-
; fgets(© and file(, will work begardle{s of the sourcm of the file.
; http://php.net/auto/deteaTmlIne-endings
;auto_detect_line_Ândings =!Off

;;;9;;;;;;;;;;;;;;;;;;; Dynamic Extensyons ;
;;;;;;;;;;;;;;;;;;;;;;ä
;"If you wish to have an extensioN"loaded automatically, use uhe following
;†syntax:
;*;   extencion=modwlename.eztansion
;
; For example, on Windows:
;M
;   extension=msqL.dll
;
; ... or under UNIX:
;
;   extension=isql.so
;
; ... or with a pathä;
;   extension=/patH/to/extension/msql.so
;
; Ig"you on,y provide the name o& the extension, PHP wilL look fov it in its
; defau,t extension directory.
;
; Windows Extensions
; Note that ODBC support is built in, so no dll†is nedded for it.
; Note tËat many DLL files arE loccted in the extensions' (HP 4) ext/ ®pHP 1)
; extension folders as well as thE separate PECL DLL download (PHP 5).
; Be"suse to appropriately set the extensi/n_dÈr directive.
-
extension=php_bz2.dLl
;extension?php_curl.dll
extens)on=php[mbstring.dll
e¯tension=qhp_exif,dll
;extension=php_fileinfo.dll
extension=php_gd2,dll
exteÓsion=php_gEttext.dll
;extensiÔn=php_gmp.dll
;extensIon=php_intl.dll
;extension=php_imap.dll
;extension=php_interbaqe.dll
;extension=0hp_ldap.dll
;exdension=xhp_mssql.ddl
;extenSion=php_mbstring,dll
3extension=php_exiF.dll " `  ; Must be after mbstring as it depends on it
extension=php_mysq,.dll
extension=php_mysQli.dll
;extension=php]kci8.dll      ; Use with Oracle(10gR2 Instant Client
;extension=php_oci8_11g.dll  ; Use with Oracle 11gR2 Instanv Chient
;extension=php_openssl.dll
;eztension=php_pdo_firebird.dll
extension=php_p‰o_mysql.dll
;mxtenrion?xhp_pdo_oci.dll
ªex|ension=php_pdo_odbc.dll-
;exte~sion=pËp_pdo_pgsql.dll
exte.siOn=php_pdo_sqlite.dll
;extension=qhp_pdo_sqlite_external.dll
;extension=php[pgSql.dll
;extension}qhp_pspell.dLl
;exten{ion=php_shmop.dll

; The MIBS data av·ilable in the PHP distribution must be installed. 
; See http;//www.php.netßmanual/en/snmp.instaLlation.php 
;extension9phpWsnmp.dll

extension=php_so·p.dll
extEnsÈon=php_socket3.dll
extension=php_sqlite3.dll
;extensikn=pËp_sybase_ct.dll
;exte.sion=pËp_tidy.dlh
extension=php_xmlbpc.dll
extension=ph_xsl.dll

;;;;;;;;;;;;;;9;;;;-
; Module Settings ;
;;;:;;;;;;;;;;;;;;;

[CLI Server]
; Whether the CLI web server uses ANSI color coding in its terminal output.
cli_server.color = On

[Date]
; Defines the default timezone used by the date functions
; http://php.net/date.timezone
date.timezone = Europe/Berlin

; http://php.net/date.default-latitude
;date.default_latitude = 31.7667

; http://php.net/date.default-longitude
;date.default_longitude = 35.2333

; http://php.net/date.sunrise-zenith
;date.sunrise_zenith = 90.583333

; http://php.net/date.sunset-zenith
;date.sunset_zenith = 90.583333

[filter]
; http://php.net/filter.default
;filter.default = unsafe_raw

; http://php.net/filter.default-flags
;filter.default_flags =

[iconv]
;iconv.input_encoding = ISO-8859-1
;iconv.internal_encoding = ISO-8859-1
;iconv.output_encoding = ISO-8859-1

[intl]
;intl.default_locale =
; This directive allows you to produce PHP errors when some error
; happens within intl functions. The value is the level of the error produced.
; Default is 0, which does not produce any errors.
;intl.error_level = E_WARNING

[sqlite]
; http://php.net/sqlite.assoc-case
;sqlite.assoc_case = 0

[sqlite3]
;sqlite3.extension_dir =

[Pcre]
;PCRE library backtracking limit.
; http://php.net/pcre.backtrack-limit
;pcre.backtrack_limit=100000

;PCRE library recursion limit.
;Please note that if you set this value to a high number you may consume all
;the available process stack and eventually crash PHP (due to reaching the
;stack size limit imposed by the Operating System).
; http://php.net/pcre.recursion-limit
;pcre.recursion_limit=100000

[Pdo]
; Whether to pool ODBC connections. Can be one of "strict", "relaxed" or "off"
; http://php.net/pdo-odbc.connection-pooling
;pdo_odbc.connection_pooling=strict

;pdo_odbc.db2_instance_name

[Pdo_mysql]
; If mysqlnd is used: Number of cache slots for the internal result set cache
; http://php.net/pdo_mysql.cache_size
pdo_mysql.cache_size = 2000

; Default socket name for local MySQL connects.  If empty, uses the built-in
; MySQL defaults.
; http://php.net/pdo_mysql.default-socket
pdo_mysql.default_socket = "MySQL"

[Phar]
; http://php.net/phar.readonly
;phar.readonly = On

; http://php.net/phar.require-hash
;phar.require_hash = On

;phar.cache_list =

[Syslog]
; Whether or not to define the various syslog variables (e.g. $LOG_PID,
; $LOG_CRON, etc.).  Turning it off is a good idea performance-wise.  In
; runtime, you can define these variables by calling define_syslog_variables().
; http://php.net/define-syslog-variables
define_syslog_variables  = Off

[mail function]
; XAMPP: Comment out this if you want to work with an SMTP Server like Mercury
 SMTP = localhost
 smtp_port = 25

; For Win32 only.
; http://php.net/sendmail-from
;sendmail_from = alee.ranjam@gmail.com

; XAMPP IMPORTANT NOTE (1): If XAMPP is installed in a base directory with spaces (e.g. c:\program files\PHP\xampp) fakemail and mailtodisk do not work correctly.
; XAMPP IMPORTANT NOTE (2): In this case please copy the sendmail or mailtodisk folder in your root folder (e.g. C:\sendmail) and use this for sendmail_path. 

; XAMPP: Comment out this if you want to work with fakemail for forwarding to your mailbox (sendmail.exe in the sendmail folder)
;sendmail_path = "\"\PHP\xampp\sendmail\sendmail.exe\" -t"

; XAMPP: Comment out this if you want to work with mailToDisk, It writes all mails in the \PHP\xampp\mailoutput folder
sendmail_path = "\PHP\xampp\mailtodisk\mailtodisk.exe"

; Force the addition of the specified parameters to be passed as extra parameters
; to the sendmail binary. These parameters will always replace the value of
; the 5th parameter to mail(), even in safe mode.
;mail.force_extra_parameters =

; Add X-PHP-Originating-Script: that will include uid of the script followed by the filename
mail.add_x_header = Off

; Log all mail() calls including the full path of the script, line #, to address and headers
;mail.log = "\PHP\xampp\php\logs\php_mail.log"

[SQL]
; http://php.net/sql.safe-mode
sql.safe_mode = Off

[ODBC]
; http://php.net/odbc.default-db
;odbc.default_db    =  Not yet implemented

; http://php.net/odbc.default-user
;odbc.default_user  =  Not yet implemented

; http://php.net/odbc.default-pw
;odbc.default_pw    =  Not yet implemented

; Controls the ODBC cursor model.
; Default: SQL_CURSOR_STATIC (default).
;odbc.default_cursortype

; Allow or prevent persistent links.
; http://php.net/odbc.allow-persistent
odbc.allow_persistent = On

; Check that a connection is still valid before reuse.
; http://php.net/odbc.check-persistent
odbc.check_persistent = On

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/odbc.max-persistent
odbc.max_persistent = -1

; Maximum number of links (persistent + non-persistent).  -1 means no limit.
; http://php.net/odbc.max-links
odbc.max_links = -1

; Handling of LONG fields.  Returns number of bytes to variables.  0 means
; passthru.
; http://php.net/odbc.defaultlrl
odbc.defaultlrl = 4096

; Handling of binary data.  0 means passthru, 1 return as is, 2 convert to char.
; See the documentation on odbc_binmode and odbc_longreadlen for an explanation
; of odbc.defaultlrl and odbc.defaultbinmode
; http://php.net/odbc.defaultbinmode
odbc.defaultbinmode = 1

;birdstep.max_links = -1

[Interbase]
; Allow or prevent persistent links.
ibase.allow_persistent = 1

; Maximum number of persistent links.  -1 means no limit.
ibase.max_persistent = -1

; Maximum number of links (persistent + non-persistent).  -1 means no limit.
ibase.max_links = -1

; Default database name for ibase_connect().
;ibase.default_db =

; Default username for ibase_connect().
;ibase.default_user =

; Default password for ibase_connect().
;ibase.default_password =

; Default charset for ibase_connect().
;ibase.default_charset =

; Default timestamp format.
ibase.timestampformat = "%Y-%m-%d %H:%M:%S"

; Default date format.
ibase.dateformat = "%Y-%m-%d"

; Default time format.
ibase.timeformat = "%H:%M:%S"

YMySQL]
; Allow abcessing, from PHp's perspecTive, local files with LOAD DATA statements; htTp://php.net/mysql.allow_local_infile
mysql.allow_local_infile = _n

; Allow or prevent`persistent links.
; http://php.net/mysql.qllow-persistent
mysql.all/w_persistent = On

; If mysqlnd is used: Number of bache slots for the internal result set cache
; http://php.net/mysql.cache_size
mysql.cache_size = 2000

; MaxiÌum number of persistent links.  -1 means no limit.
; httq://php.net/mysql.max-pessistent
mysql.max_persistent = -1

: Maximum number of links (persistent + non-persistent).  -1 means no limit.
; http://php.net/myÛql.max)links
mysql.max_links = -1

; Default port numfer for mysql_connect().  Mf unset, mysql_conne#t() will use
; the $MYSQL_TCPWPORT or the mysql-tcp entry in /mtc/servicec or the
; com0ile-time value defined MYSQL_PORT (in`that order).  Win32 will only look
; at MYSQL_PORT.ä; http//php.nep/mysql.default-port
mysql.default_port = 3306

; defiunt socket ncme for local MySQL connects.  If empty, uses the built-in
; MySQL defaults.
; http://php.net/mysql.default-socket
mysql.default_socket = "MySQL"

; Default host for mysql_connect() (doesn't apply in safe mode).
; http://php.net/mysql.default-host
mysql.default_host =

; Default user for mysql_connect() (doesn't apply in safe mode).
; http://php.net/mysql.default-user
mysql.default_user =

; Default password for mysql_connect() (doesn't apply in safe mode).
; Note that this is generally a *bad* idea to store passwords in this file.
; *Any* user with PHP access can run 'echo get_cfg_var("mysql.default_password")
; and reveal this password!  And of course, any users with read access to this
; file will be able to reveal the password as well.
; http://php.net/mysql.default-password
mysql.default_password =

; Maximum time (in seconds) for connect timeout. -1 means no limit
; http://php.net/mysql.connect-timeout
mysql.connect_timeout = 3

; Trace mode. When trace_mode is active (=On), warnings for table/index scans and
; SQL-Errors will be displayed.
; http://php.net/mysql.trace-mode
mysql.trace_mode = Off

[MySQLi]

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/mysqli.max-persistent
mysqli.max_persistent = -1

; Allow accessing, from PHP's perspective, local files with LOAD DATA statements
; http://php.net/mysqli.allow_local_infile
mysqli.allow_local_infile = On

; Allow or prevent persistent links.
; http://php.net/mysqli.allow-persistent
mysqli.allow_persistent = On

; Maximum number of links.  -1 means no limit.
; http://php.net/mysqli.max-links
mysqli.max_links = -1

; If mysqlnd is used: Number of cache slots for the internal result set cache
; http://php.net/mysqli.cache_size
mysqli.cache_size = 2000

; Default port number for mysqli_connect().  If unset, mysqli_connect() will use
; the $MYSQL_TCP_PORT or the mysql-tcp entry in /etc/services or the
; compile-time value defined MYSQL_PORT (in that order).  Win32 will only look
; at MYSQL_PORT.
; http://php.net/mysqli.default-port
mysqli.default_port = 3306

; Default socket name for local MySQL connects.  If empty, uses the built-in
; MySQL defaults.
; http://php.net/mysqli.default-socket
mysqli.default_socket = "MySQL"

; Default host for mysql_connect() (doesn't apply in safe mode).
; http://php.net/mysqli.default-host
mysqli.default_host =

; Default user for mysql_connect() (doesn't apply in safe mode).
; http://php.net/mysqli.default-user
mysqli.default_user =

; Default password for mysqli_connect() (doesn't apply in safe mode).
; Note that this is generally a *bad* idea to store passwords in this file.
; *Any* user with PHP access can run 'echo get_cfg_var("mysqli.default_pw")
; and reveal this password!  And of course, any users with read access to this
; file will be able to reveal the password as well.
; http://php.net/mysqli.default-pw
mysqli.default_pw =

; Allow or prevent reconnect
mysqli.reconnect = Off

[mysqlnd]
; Enable / Disable collection of general statstics by mysqlnd which can be
; used to tune and monitor MySQL operations.
; http://php.net/mysqlnd.collect_statistics
mysqlnd.collect_statistics = On

; Enable / Disable collection of memory usage statstics by mysqlnd which can be
; used to tune and monitor MySQL operations.
; http://php.net/mysqlnd.collect_memory_statistics
mysqlnd.collect_memory_statistics = Off

; Size of a pre-allocated buffer used when sending commands to MySQL in bytes.
; http://php.net/mysqlnd.net_cmd_buffer_size
;mysqlnd.net_cmd_buffer_size = 2048

; Size of a pre-allocated buffer used for reading data sent by the server in
; bytes.
; http://php.net/mysqlnd.net_read_buffer_size
;mysqlnd.net_read_buffer_size = 32768

[OCI8]

; Connection: Enables privileged connections using external
; credentials (OCI_SYSOPER, OCI_SYSDBA)
; http://php.net/oci8.privileged-connect
;oci8.privileged_connect = Off

; Connection: The maximum number of persistent OCI8 connections per
; process. Using -1 means no limit.
; http://php.net/oci8.max-persistent
;oci8.max_persistent = -1

; Connection: The maximum number of seconds a process is allowed to
; maintain an idle persistent connection. Using -1 means idle
; persistent connections will be maintained forever.
; http://php.net/oci8.persistent-timeout
;oci8.persistent_timeout = -1

; Connection: The number of seconds that must pass before issuing a
; ping during oci_pconnect() to check the connection validity. When
; set to 0, each oci_pconnect() will cause a ping. Using -1 disables
; pings completely.
; http://php.net/oci8.ping-interval
;oci8.ping_interval = 60

; Connection: Set this to a user chosen connection class to be used
; for all pooled server requests with Oracle 11g Database Resident
; Connection Pooling (DRCP).  To use DRCP, this value should be set to
; the same string for all web servers running the same application,
; the database pool must be configured, and the connection string must
; specify to use a pooled server.
;oci8.connection_class =

; High Availability: Using On lets PHP receive Fast Application
; Notification (FAN) events generated when a database node fails. The
; database must also be configured to post FAN events.
;oci8.events = Off

; Tuning: This option enables statement caching, and specifies how
; many statements to cache. Using 0 disables statement caching.
; http://php.net/oci8.statement-cache-size
;oci8.statement_cache_size = 20

; Tuning: Enables statement prefetching and sets the default number of
; rows that will be fetched automatically after statement execution.
; http://php.net/oci8.default-prefetch
;oci8.default_prefetch = 100

; Compatibility. Using On means oci_close() will not close
; oci_connect() and oci_new_connect() connections.
; http://php.net/oci8.old-oci-close-semantics
;oci8.old_oci_close_semantics = Off

[PostgresSQL]
; Allow or prevent persistent links.
; http://php.net/pgsql.allow-persistent
pgsql.allow_persistent = On

; Detect broken persistent links always with pg_pconnect().
; Auto reset feature requires a little overheads.
; http://php.net/pgsql.auto-reset-persistent
pgsql.auto_reset_persistent = Off

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/pgsql.max-persistent
pgsql.max_persistent = -1

; Maximum number of links (persistent+non persistent).  -1 means no limit.
; http://php.net/pgsql.max-links
pgsql.max_links = -1

; Ignore PostgreSQL backends Notice message or not.
; Notice message logging require a little overheads.
; http://php.net/pgsql.ignore-notice
pgsql.ignore_notice = 0

; Log PostgreSQL backends Noitce message or not.
; Unless pgsql.ignore_notice=0, module cannot log notice message.
; http://php.net/pgsql.log-notice
pgsql.log_notice = 0

[Sybase-CT]
; Allow or prevent persistent links.
; http://php.net/sybct.allow-persistent
sybct.allow_persistent = On

; Maximum number of persistent links.  -1 means no limit.
; http://php.net/sybct.max-persistent
sybct.max_persistent = -1

; Maximum number of links (persistent + non-persistent).  -1 means no limit.
; http://php.net/sybct.max-links
sybct.max_links = -1

; Minimum server message severity to display.
; http://php.net/sybct.min-server-severity
sybct.min_server_severity = 10

; Minimum client message severity to display.
; http://php.net/sybct.min-client-severity
sybct.min_client_severity = 10

; Set per-context timeout
; http://php.net/sybct.timeout
;sybct.timeout=

;sybct.packet_size

; The maximum time in seconds to wait for a connection attempt to succeed before returning failure.
; Default: one minute
;sybct.login_timeout=

; The name of the host you claim to be connecting from, for display by sp_who.
; Default: none
;sybct.hostname=

; Allows you to define how often deadlocks are to be retried. -1 means "forever".
; Default: 0
;sybct.deadlock_retry_count=

[bcmath]
; Number of decimal digits for all bcmath functions.
; http://php.net/bcmath.scale
bcmath.scale = 0

[browscap]
; http://php.net/browscap
browscap = "\PHP\xampp\php\extras\browscap.ini"

[Session]
; Handler used to store/retrieve data.
; http://php.net/session.save-handler
session.save_handler = files

; Argument passed to save_handler.  In the case of files, this is the path
; where data files are stored. Note: Windows users have to change this
; variable in order to use PHP's session functions.
;
; The path can be defined as:
;
;     session.save_path = "N;/path"
;
; where N is an integer.  Instead of storing all the session files in
; /path, what this will do is use subdirectories N-levels deep, and
; store the session data in those directories.  This is useful if you
; or your OS have problems with lots of files in one directory, and is
; a more efficient layout for servers that handle lots of sessions.
;
; NOTE 1: PHP will not create this directory structure automatically.
;         You can use the script in the ext/session dir for that purpose.
; NOTE 2: See the section on garbage collection below if you choose to
;         use subdirectories for session storage
;
; The file storage module creates files using mode 600 by default.
; You can change that by using
;
;     session.save_path = "N;MODE;/path"
;
; where MODE is the octal representation of the mode. Note that this
; does not overwrite the process's umask.
; http://php.net/session.save-path
session.save_path = "\PHP\xampp\tmp"

; Whether to use cookies.
; http://php.net/session.use-cookies
session.use_cookies = 1

; http://php.net/session.cookie-secure
;session.cookie_secure =

; This option forces PHP to fetch and use a cookie for storing and maintaining
; the session id. We encourage this operation as it's very helpful in combatting
; session hijacking when not specifying and managing your own session id. It is
; not the end all be all of session hijacking defense, but it's a good start.
; http://php.net/session.use-only-cookies
session.use_only_cookies = 0

; Name of the session (used as cookie name).
; http://php.net/session.name
session.name = PHPSESSID

; Initialize session on request startup.
; http://php.net/session.auto-start
session.auto_start = 0

; Lifetime in seconds of cookie or, if 0, until browser is restarted.
; http://php.net/session.cookie-lifetime
session.cookie_lifetime = 0

; The path for which the cookie is valid.
; http://php.net/session.cookie-path
session.cookie_path = /

; The domain for which the cookie is valid.
; http://php.net/session.cookie-domain
session.cookie_domain =

; Whether or not to add the httpOnly flag to the cookie, which makes it inaccessible to browser scripting languages such as JavaScript.
; http://php.net/session.cookie-httponly
session.cookie_httponly =

; Handler used to serialize data.  php is the standard serializer of PHP.
; http://php.net/session.serialize-handler
session.serialize_handler = php

; Defines the probability that the 'garbage collection' process is started
; on every session initialization. The probability is calculated by using
; gc_probability/gc_divisor. Where session.gc_probability is the numerator
; and gc_divisor is the denominator in the equation. Setting this value to 1
; when the session.gc_divisor value is 100 will give you approximately a 1% chance
; the gc will run on any give request.
; Default Value: 1
; Development Value: 1
; Production Value: 1
; http://php.net/session.gc-probability
session.gc_probability = 1

; Defines the probability that the 'garbage collection' process is started on every
; session initialization. The probability is calculated by using the following equation:
; gc_probability/gc_divisor. Where session.gc_probability is the numerator and
; session.gc_divisor is the denominator in the equation. Setting this value to 1
; when the session.gc_divisor value is 100 will give you approximately a 1% chance
; the gc will run on any give request. Increasing this value to 1000 will give you
; a 0.1% chance the gc will run on any give request. For high volume production servers,
; this is a more efficient approach.
; Default Value: 100
; Development Value: 1000
; Production Value: 1000
; http://php.net/session.gc-divisor
session.gc_divisor = 1000

; After this number of seconds, stored data will be seen as 'garbage' and
; cleaned up by the garbage collection process.
; http://php.net/session.gc-maxlifetime
session.gc_maxlifetime = 1440

; NOTE: If you are using the subdirectory option for storing session files
;       (see session.save_path above), then garbage collection does *not*
;       happen automatically.  You will need to do your own garbage
;       collection through a shell script, cron entry, or some other method.
;       For example, the following script would is the equivalent of
;       setting session.gc_maxlifetime to 1440 (1440 seconds = 24 minutes):
;          cd /path/to/sessions; find -cmin +24 | xargs rm

; PHP 4.2 and less have an undocumented feature/bug that allows you to
; to initialize a session variable in the global scope.
; PHP 4.3 and later will warn you, if this feature is used.
; You can disable the feature and the warning separately. At this time,
; the warning is only displayed, if bug_compat_42 is enabled. This feature
; introduces some serious security problems if not handled correctly. It's
; recommended that you do not use this feature on production servers. But you
; should enable this on development servers and enable the warning as well. If you
; do not enable the feature on development servers, you won't be warned when it's
; used and debugging errors caused by this can be difficult to track down.
; Default Value: On
; Development Value: On
; Production Value: Off
; http://php.net/session.bug-compat-42
session.bug_compat_42 = On

; This setting controls whether or not you are warned by PHP when initializing a
; session value into the global space. session.bug_compat_42 must be enabled before
; these warnings can be issued by PHP. See the directive above for more information.
; Default Value: On
; Development Value: On
; Production Value: Off
; http://php.net/session.bug-compat-warn
session.bug_compat_warn = On

; Check HTTP Referer to invalidate externally stored URLs containing ids.
; HTTP_REFERER has to contain this substring for the session to be
; considered as valid.
; http://php.net/session.referer-check
session.referer_check =

; How many bytes to read from the file.
; http://php.net/session.entropy-length
session.entropy_length = 0

; Specified here to create the session id.
; http://php.net/session.entropy-file
; Defaults to /dev/urandom
; On systems that don't have /dev/urandom but do have /dev/arandom, this will default to /dev/arandom
; If neither are found at compile time, the default is no entropy file.
; On windows, setting the entropy_length setting will activate the
; Windows random source (using the CryptoAPI)
;session.entropy_file = /dev/urandom
session.entropy_file =

; http://php.net/session.entropy-length
;session.entropy_length = 16

; Set to {nocache,private,public,} to determine HTTP caching aspects
; or leave this empty to avoid sending anti-caching headers.
; http://php.net/session.cache-limiter
session.cache_limiter = nocache

; Document expires after n minutes.
; http://php.net/session.cache-expire
session.cache_expire = 180

; trans sid support is disabled by default.
; Use of trans sid may risk your users security.
; Use this option with caution.
; - User may send URL contains active session ID
;   to other person via. email/irc/etc.
; - URL that contains active session ID may be stored
;   in publically accessible computer.
; - User may access your site with the same session ID
;   always using URL stored in browser's history or bookmarks.
; http://php.net/session.use-trans-sid
session.use_trans_sid = 0

; Select a hash function for use in generating session ids.
; Possible Values
;   0  (MD5 128 bits)
;   1  (SHA-1 160 bits)
; This option may also be set to the name of any hash function supported by
; the hash extension. A list of available hashes is returned by the hash_algos()
; function.
; http://php.net/session.hash-function
session.hash_function = 0

; Define how many bits are stored in each character when converting
; the binary hash data to something readable.
; Possible values:
;   4  (4 bits: 0-9, a-f)
;   5  (5 bits: 0-9, a-v)
;   6  (6 bits: 0-9, a-z, A-Z, "-", ",")
; Default Value: 4
; Development Value: 5
; Production Value: 5
; http://php.net/session.hash-bits-per-character
session.hash_bits_per_character = 5

; The URL rewriter will look for URLs in a defined set of HTML tags.
; form/fieldset are special; if you include them here, the rewriter will
; add a hidden <input> field with the info which is otherwise appended
; to URLs.  If you want XHTML conformity, remove the form entry.
; Note that all valid entries require a "=", even if no value follows.
; Default Value: "a=href,area=href,frame=src,form=,fieldset="
; Development Value: "a=href,area=href,frame=src,input=src,form=fakeentry"
; Production Value: "a=href,area=href,frame=src,input=src,form=fakeentry"
; http://php.net/url-rewriter.tags
url_rewriter.tags = "a=href,area=href,frame=src,input=src,form=fakeentry"

; Enable upload progress tracking in $_SESSION
; Default Value: On
; Development Value: On
; Production Value: On
; http://php.net/session.upload-progress.enabled
;session.upload_progress.enabled = On

; Cleanup the progress information as soon as all POST data has been read
; (i.e. upload completed).
; Default Value: On
; Development Value: On
; Production Value: On
; http://php.net/session.upload-progress.cleanup
;session.upload_progress.cleanup = On

; A prefix used for the upload progress key in $_SESSION
; Default Value: "upload_progress_"
; Development Value: "upload_progress_"
; Production Value: "upload_progress_"
; http://php.net/session.upload-progress.prefix
;session.upload_progress.prefix = "upload_progress_"

; The index name (concatenated with the prefix) in $_SESSION
; containing the upload progress information
; Default Value: "PHP_SESSION_UPLOAD_PROGRESS"
; Development Value: "PHP_SESSION_UPLOAD_PROGRESS"
; Production Value: "PHP_SESSION_UPLOAD_PROGRESS"
; http://php.net/session.upload-progress.name
;session.upload_progress.name = "PHP_SESSION_UPLOAD_PROGRESS"

; How frequently the upload progress should be updated.
; Given either in percentages (per-file), or in bytes
; Default Value: "1%"
; Development Value: "1%"
; Production Value: "1%"
; http://php.net/session.upload-progress.freq
;session.upload_progress.freq =  "1%"

; The minimum delay between updates, in seconds
; Default Value: 1
; Development Value: 1
; Production Value: 1
; http://php.net/session.upload-progress.min-freq
;session.upload_progress.min_freq = "1"

[MSSQL]
; Allow or prevent persistent links.
mssql.allow_persistent = On

; Maximum number of persistent links.  -1 means no limit.
mssql.max_persistent = -1

; Maximum number of links (persistent+non persistent).  -1 means no limit.
mssql.max_links = -1

; Minimum error severity to display.
mssql.min_error_severity = 10

; Minimum message severity to display.
mssql.min_message_severity = 10

; Compatibility mode with old versions of PHP 3.0.
mssql.compatability_mode = Off

; Connect timeout
;mssql.connect_timeout = 5

; Query timeout
;mssql.timeout = 60

; Valid range 0 - 2147483647.  Default = 4096.
;mssql.textlimit = 4096

; Valid range 0 - 2147483647.  Default = 4096.
;mssql.textsize = 4096

; Limits the number of records in each batch.  0 = all records in one batch.
;mssql.batchsize = 0

; Specify how datetime and datetim4 columns are returned
; On => Returns data converted to SQL server settings
; Off => Returns values as YYYY-MM-DD hh:mm:ss
;mssql.datetimeconvert = On

; Use NT authentication when connecting to the server
mssql.secure_connection = Off

; Specify max number of processes. -1 = library default
; msdlib defaults to 25
; FreeTDS defaults to 4096
;mssql.max_procs = -1

; Specify client character set.
; If empty or not set the client charset from freetds.comf is used
; This is only used when compiled with FreeTDS
;mssql.charset = "ISO-8859-1"

[Assertion]
; Assert(expr); active by default.
; http://php.net/assert.active
;assert.active = On

; Issue a PHP warning for each failed assertion.
; http://php.net/assert.warning
;assert.warning = On

; Don't bail out by default.
; http://php.net/assert.bail
;assert.bail = Off

; User-function to be called if an assertion fails.
; http://php.net/assert.callback
;assert.callback = 0

; Eval the expression with current error_reporting().  Set to true if you want
; error_reporting(0) around the eval().
; http://php.net/assert.quiet-eval
;assert.quiet_eval = 0

[COM]
; path to a file containing GUIDs, IIDs or filenames of files with TypeLibs
; http://php.net/com.typelib-file
;com.typelib_file =

; allow Distributed-COM calls
; http://php.net/com.allow-dcom
;com.allow_dcom = true

; autoregister constants of a components typlib on com_load()
; http://php.net/com.autoregister-typelib
;com.autoregister_typelib = true

; register constants casesensitive
; http://php.net/com.autoregister-casesensitive
;com.autoregister_casesensitive = false

; show warnings on duplicate constant registrations
; http://php.net/com.autoregister-verbose
;com.autoregister_verbose = true

; The default character set code-page to use when passing strings to and from COM objects.
; Default: system ANSI code page
;com.code_page=

[mbstring]
; language for internal character representation.
; http://php.net/mbstring.language
;mbstring.language = Japanese

; internal/script encoding.
; Some encoding cannot work as internal encoding.
; (e.g. SJIS, BIG5, ISO-2022-*)
; http://php.net/mbstring.internal-encoding
;mbstring.internal_encoding = EUC-JP

; http input encoding.
; http://php.net/mbstring.http-input
;mbstring.http_input = auto

; http output encoding. mb_output_handler must be
; registered as output buffer to function
; http://php.net/mbstring.http-output
;mbstring.http_output = SJIS

; enable automatic encoding translation according to
; mbstring.internal_encoding setting. Input chars are
; converted to internal encoding by setting this to On.
; Note: Do _not_ use automatic encoding translation for
;       portable libs/applications.
; http://php.net/mbstring.encoding-translation
;mbstring.encoding_translation = Off

; automatic encoding detection order.
; auto means
; http://php.net/mbstring.detect-order
;mbstring.detect_order = auto

; substitute_character used when character cannot be converted
; one from another
; http://php.net/mbstring.substitute-character
;mbstring.substitute_character = none;

; overload(replace) single byte functions by mbstring functions.
; mail(), ereg(), etc are overloaded by mb_send_mail(), mb_ereg(),
; etc. Possible values are 0,1,2,4 or combination of them.
; For example, 7 for overload everything.
; 0: No overload
; 1: Overload mail() function
; 2: Overload str*() functions
; 4: Overload ereg*() functions
; http://php.net/mbstring.func-overload
;mbstring.func_overload = 0

; enable strict encoding detection.
;mbstring.strict_detection = Off

; This directive specifies the regex pattern of content types for which mb_output_handler()
; is activated.
; Default: mbstring.http_output_conv_mimetype=^(text/|application/xhtml\+xml)
;mbstring.http_output_conv_mimetype=

; Allows to set script encoding. Only affects if PHP is compiled with --enable-zend-multibyte
; Default: ""
;mbstring.script_encoding=

[gd]
; Tell the jpeg decode to ignore warnings and try to create
; a gd image. The warning will then be displayed as notices
; disabled by default
; http://php.net/gd.jpeg-ignore-warning
;gd.jpeg_ignore_warning = 0

[exif]
; Exif UNICODE user comments are handled as UCS-2BE/UCS-2LE and JIS as JIS.
; With mbstring support this will automatically be converted into the encoding
; given by corresponding encode setting. When empty mbstring.internal_encoding
; is used. For the decode settings you can distinguish between motorola and
; intel byte order. A decode setting cannot be empty.
; http://php.net/exif.encode-unicode
;exif.encode_unicode = ISO-8859-15

; http://php.net/exif.decode-unicode-motorola
;exif.decode_unicode_motorola = UCS-2BE

; http://php.net/exif.decode-unicode-intel
;exif.decode_unicode_intel    = UCS-2LE

; http://php.net/exif.encode-jis
;exif.encode_jis =

; http://php.net/exif.decode-jis-motorola
;exif.decode_jis_motorola = JIS

; http://php.net/exif.decode-jis-intel
;exif.decode_jis_intel    = JIS

[Tidy]
; The path to a default tidy configuration file to use when using tidy
; http://php.net/tidy.default-config
;tidy.default_config = "\PHP\xampp\php\extras\default.tcfg"

; Should tidy clean and repair output automatically?
; WARNING: Do not use this option if you are generating non-html content
; such as dynamic images
; http://php.net/tidy.clean-output
tidy.clean_output = Off

[soap]
; Enables or disables WSDL caching feature.
; http://php.net/soap.wsdl-cache-enable‰
soip.wsdl_cAche_enabled=1

{ Sets the"directory name where SGAP extension will put cache files.
; http://xhp..et/soap.wsdl-cache-dis
soap*wsdl_cache_dirΩ"/tmp"

; (time to(live) Sets the numbdr of second while cached file"will bE used
3 instead of originam one.
; http://Php.net/soap.wsdl-ccbhe-ttl
soap.wsdl_caahe_ttl=86410

; Sets the size of$the cache limit/ (Max. number of WSDL files |o cache)
Soap.wsdl_cache_limit = 5

Zsysvshm]
; A defauÏt si~e mF the Ûhared m%}ory(sggment
;sysvrhm/Èlit_}em = 10000
ä[leap]
; Sets the mazimum number of open links or -1 for unlimited.
ldap.max_lioks = -1
[mcryxt]
; For mÔre infozmatioj about mcrypt settings see http://p(p.net/mcrypt-module-open

; Direstory where to loat ocrypt algor)thms
; Ddfault: Compilad in into libmcrypt (usually /usr/local/lib/,ibmcrypt)
?ocrypt.algorithms_dir=

; DiÚeatÔry wherg to load marypt modes
; Default: Compiled in into`liblcrypt (usually /usr/local/lib/nibÌcrypt)
;m„r9pt.modes_dir5

[dbe]
;dba/default_hqndler=
[FrontBase]
;fbsql.allowüpmrsistent = On
;fbsqÏ.!uuocommit = On
;fbsql.show_timestamp_decimqls = Off
;fbSql.defaulT_database =	
;fbsql.defaund_database_p!ssword =
;fbsql.default_host Ω
;&bsql.`afault_passwort =
;fbsql.dEfault_user = #_SYSTEM"
;fbsql.generate_warnings = Off
;fbsql.max_connectaons = 12<
;fbsqm.max_links = 128
;bfsql.max_peraistent = -1
3fbsql.max_results = 12∏

[mime_magic]
mime_m!gic.magicfmle = *\PHP|xampp\php\extras\magmc.mime"
+mIme_magic.debug = On

[eAccelerat/r]
;zend_extension = "\PHP‹xaopp\php\ext\php_eaCcelerator_ts.dll"

; The amount of shared memory (in megabytes) thaÙ eAccelebator will use.M
; "0" means OW default. Defaul4 valwe is "0".
eaccelerator.shm_s)ze =("0"
; The directory that is used fkr disk cache. eAccelerauoz stores precompiled
; code, se{sion data, content a.d user endrieQ  here. The same data can†be
{ store‰ il 3hared memory alsm (for more quiak accdÛs). Defaelt value is
; "/tmp/Âaccelerator".
eaccelerator.cacheWdir = "\pHP\xampp\tmp"

; Enables or ‰isables eAcCelerator. hou,d bd "1" f/r unabling or
; "0" fnr disabling. Defaumt value is "±".
eaccelerator.enable = "1"

; Enables or$dÈsables debug loggÈng. Setting this to 1 will print information
; to the log file about the cach hits of a fine*
eacce|erator/debug = ∞

; Sat the$log file for eaccdlerator. When this op|ion is.'t set then the datc
; wiLl be logged to stdmrr;eaccelgrator.do'_file = "\PHP\xampp\apache\logs\eaccelera4or.log"; Ânab,es kr disables PHP file modificatkon checkkng. Slould Be "1"
; for enabling or "0# fmr disabling. You should set iv to "1" if you want
; to recompihe(THP files after modificAtion. DefauÏt value ic "1".
eaCCelerator.check_otime = "1"

; Determine which PHP files must be cached.0You(may specify tje number of
; patterns (for example "*.phx **phtml") hich specyfies to cache or
; not tn cache. If pattern starts with uhe cxaracter "!", iT mdans to ignore
; files which are matched by the following pattern. Default value is ¢" th·t
; means - Adl PHP scripts wild be cached.
eacaelÂrator.filter = ""

; Disables put6ing large valu%s ijto shared memory by "eacceleratOR_put()"
; function.
; It in‰icates the largest allowed size in bxtes (10240,`10K, 1M). The "0"
; disables txe limit. Default value(is "0".
eaccelerator.shm_max = "0"ç

; When eAccelerator fqils to get sharEd memory fkr new script it remves
; all scripts which we2e not"accessed!at last "shmOtt," seconds from shared
; mumory. Default value is "0" that means - don't remove any files from
; shareD memory.
eaccelerator.shm[ttl = "0"

; When eAcgelerator fails to get shared memgsy for new scriqt it trYes to
;†semove old script if the previous try was ma‰e more then "3hm_pruneﬂpeÚiod"
; seconds ago. Defauht talte i3 "0" Ùhat means - don't try to remove any
; files from whared memory.
eaccelerator.shm_prune_period 5 *0"

; EnabÏes or disables caching of compiled scripts on disj. It has n effect
; on se3sion data and content caching.
; Default!value is "0" that means - use disk and sharev memry for caching.
eaccelerator.shm_only = "0"

[XDgbug]
;zgnd_extension = "\PHP\xampp\php\ext|php_xdebug.dl,"

; xdebug.auto_trace
; Type: booluan, DefaulT walue: 0
; When this setting is"set to Ôn, the tracing of function calls will be enarled just before the
; scrit is rın. Tjkc makes it possiblu†to tracu cmde in!thE auto_prepend_vile.;xdebug.auto_trace = 0

3 x`ebug.cohlegt_includas
; Type: "oolean,(Default value: 1
; This seÙTing, defaulting to On, controls whether XdebtG should write thd filename used in include
; (), inclwde_once(), require() or require_oncE()`to the tra„e files.
;xdebug.collect_includes = 1

; xdebug.collect_paraÌs
; Type: integer, Default value: 0M
;
; Thas setting, defaulting to  , co~trols whetheÚ XdebU' should collect the"par·metezs a·sse‰ to
; functions when a function call(is recorded in`either the function!tracE or the stack vrace.
;xdebug.kollect_params = 0
; zdÂbug.collect_return; Uype: booleao, D%fault value: 0-
; Phis setting, dgfaulting†to Off,0controls whmther Xdebug should`write the return vahua Ôf fun#tion
; calls to thu trace files.
;xdebug.cÔllect_return = 0

; xeebug.collect_varÛ
; Type: "oolman, DefauLt value: Off
; This setting tells Xdm‚ug to gather information aboutwhich vqbaables are uset in a certain scope.
; This analysis can be quite slow as X`ebug has to reversd eNgineer PH–'s opcoda arRays. This setting
;"will not rdcord which values the different variables have, for that use xde`ug.collect_paraos. ThiS
; setting needs to be enabled only if yoq wisi to use x‰ebug_get_declared_vars().
+xdebug.collect_vars = "Off"

; xderugÆdefault_encble
; Type: boolean, E%faQlt ~·lue: On
; If uhis settine hs On then stacktraces will(be shown by default on an error event. YÔu c`n disabÏe; showiog stackdraces from your code With xdebug_disabme*). As thhs is one of the basic nunctions of
; Xdebug( it is adv)sable to leave"this setting$seT to$'On'.
;xdeBug.deÊault_enable = "On"

; xdebug.dump.*; Vype: string, Default valu%: Empty
; * = COOKIE, FILES, GET, POST, REQ’EST, SARVER, SESSIO. These seven settings control(which data
;!from the supebghobals is s(kwn when an`error situathon occurs. Aaah php.ini setting can(consmst of
; a comma seperatgd list of variables from this superglobal tÔ dump, but make Ûure you do not add
; spaces in`this setthng. In or`er tm dump t`e REMOT≈_ADZ and the REQUEST_METHOD when"an e2rnr; occurs, add this setting:
;
; xdebug.dump.SERVER  REMOTE_ADDR,REQUEST_METHOT
; xdebug.dump.SERVER = REMOTE_ADDR,REQUESV_METHOD
;xdebugdump.COOKIE < ""
;xdebug.dump.FILES = ""
;xde ug.dump*GET = ""
;xdebug.dump.POST = ""
;xdebug.dump*REQUEST = "";xde`tg.dump.SERVER = ""
;xdabug.demp.SESSION = ""

; xdejug.dump_globals
; Type; boo,ean, Default valwe: 1
; Controls whether the values of(the superglobals as def)ned by the xdmbug.dump.* settings whould be
; shown oÚ not.
;xdebug.dumq_gmobaÏs = 1
; xdeBug.dump_onceç
; TypE: booleAn, Default value: 1
;$Controls whether the v%lu%s od(thd superglobals should be dumped on all error situations (set0to
> Off) or only on the first (set tÔ"On).
;xdebug.dump[once = 1

; xdebug.dump_undefined*; Type: boolean, Default value:(0
; Ib yku want to duep undefined values from the superglobals you should set$this setthng to On,
;"otherwise leave it set t/ Off.
+xdebug.dump_undefined 9 0

; ydebug*extendeD_info
; Type: intÂges, Default valıe 1
; /ntrols whether Xdebug should enforce 'extended_info' mode for the PHP parse; this alloww Xdebug
; to do file/li~e breakpoyntc with thÂ remotm de"ugger. When tracing or proniling qcripts you; gundrally want to"turn mff t(iw option as PHP's generated oparraqs will increasE with about a third
; of txg size slowing down your scripts."This setting can not be set in your scripts with kni_smt(),
; but Ônly0in pHp.ini.
;xeebug.%xtended_info = 0

; xdefug.file_link_format
; Type: string, Default value: *empty string* , Intro$uced in Xdebug 2.1;
; This satting determinms the`fosmat of the linis that are made"in tje display of stack tracÂs whmre
; fÈle names are used. ThiS allows IDEs`to set up a link-protocol that makes it possible to!go
; directly!Ùo a line and file by clicking on the filenames t`at Xdebqg shows in`stack traces>
{xdebugÆfihe_link_format = ""

; xdebwg.idekey
; Type: string, Default value: *complex*
; ContrÔls which IDE Key X‰ebug should pass on t/ the DBGp debugger handler. The default is based ol
; environment settings. First the Envi2onment 3etting DRGP_IDEKEY is consulted, Then USER and as last
; USERNAME. The default is†set to†thd first environment ~ariable that is found. If noje could be
; fouNd the setting has as default ''.
;xdebug.id'key 9 ""

;ydebug.lcnual_url
; Type: string, Default value: xttp:/Øwww.php.netJ; This is the base url fnr the links frm the function"traces and error message to the manual$pages
; of the function nrom the messqge.0It is advisable t set |his settinw†to use the closest mirror/
;xdebug.manual_ır| = "Ëttp://www.php.n%t"

; x$ebug.max_nesting_level
;(Type: integer, Def!ult value* 100
; √ontrols the protection mecjaniwm for infhnite†recursion prtection. The value of this setting is
; the Ìazimum level of neSted functions that are allowed before the script will0be aborted.
;xderug.miy_nesting_le~el = 100

; xdebug.overload_var_`ump
; Ty0e: boolean, Lefa5lt!value: 10, Intboduced in Xdebug 2.1
; By default Xdebug oveÚloqds var_dump() with its own imp2vgd veb{ion foÚ distlaying variabmas when
; the html_errors!php.ini settiog is set to 1. In case you do(nkt want that, you can set this setting
; to 0, but check first if it's not smarter to turn off html_errors.
;xdebug.overload_vaÚ_dump = 1

+ xdebug.profiler_append
; Type:"integer, Default value: 0
; When this sedthng is set to 1, profiler files will not fe overwrittendwhen a new req}est would map
9 do the same fil% (depnding on the xdebug.profiner_outpu4Name setting. Instead the file will bd
; appended Ùo with phe new profile.
xderug.profiler^append = 0

; x`ebug.profile2Oenable
; Type: integer, Default value: 0
; Efables Xdebug's profiler which c˙eates files in!the profile output dyr%ctory. Those files can0be
; read by KCacheGrint to visuadize youv data. Thks setthng can not be set in your script with inisetç
; ().
xdebug.profiler_enable = 0

; xdebug.pro&iler_enable_trigger
; Type: integer, Default value: 0
; When this setting iw set to ±, you can trigger the†generation of profiler files by using dhe
; \dMBUG_TROFILE GET/OST parameter. This will 4hen write the rofaner data to defined directory.
xdebug.profiler_enable_trigcer = 0
; xdebug.profiler_output_dir
; Type: string, Default value: /tip
; Tle directory"where the profiler owtput wil, be written0to, Make sure vhat the user who the"PHP
; will be running as has write!permissions to"that directory. This settiNg can not ce set!mn your
3 script with ini_seth).
xdebug&Profiler_output^dir = "\PHP\xampp\tmp"

; xfebeg.profiler_output_name
;0Type: string, Default ~alue: cachegrind.out.%p
;
; This setting d%termines the name of the file that is used to dump praces inÙo. The setting
; spıcifies tie format with format specifiers, very similar to sprinuf() and strftime(). Th%re are
; sevaral fmrmat specifiers t`at can be used To format t`e file name.
;ä; See the xdebug.traceOoutput_name documentation for the supqorted specifiers.
xdebug.profiner_output_name = "xdubug_profile.%R::%u"
; xdebug.remote_autostabt
; Type: coolean, Default value: 0
; NormalÏy yo} need to use a specific HTTP(GET/POST variable to stabt r%mote0debugging ®see Remote
; Debugging). When ıhis setting Ìs set to 'On' Xdebug wiÏl always attempt to start a rgiotÂ dabugging
;`session ajd try to connect to a client, even if tËe GET/PœST/COOKIE variable was not prewent.;xdebug.remote_auuostart = 0

;$xdebuGnremotd_encble
; T˘pe: boolean, Default value: 0
; Dhis switch conTrols0whether Xdebug rhould try to contact i debuc client which is listening Ôn the
; host and pOrv as set with the setdings xdeb}g.remotÂ_host and xdebug.remote^port. If a!cONnection
;!caj no| be established the(script will just0Continue as if this setting was Off.ä;xdebug.remote_enable = 0

; xdebug.vemote_handler
; Type: strhng, Default value: dbgp
; Can be either 'php3/ which$selects the old pHP03 styl% defugger ouuput, 'wdb' which anables the GDB
; like debugger interface or 'dbgp' - uhe$bvand new debugger protocol. The DBGp protocl is more
; widely supported by clients> See more information in the"invroduction for Remotw Debugging.
;xtubug.remote_handler = "dbgp"

;†xdebug.remotm_host
; Type: string, Defqult value: localhost
; [elects t`e host where the $ebug client is runnhng, y/u can either uce a host n!me op an IP
; address.
;xdebug.remote_hst = "localhost"

; xdebug.remote_log
; Type: string, Default value: none
; If set to a value it is used as filename to a file to which ahl remote de‚ugger communhcations are
; logged. Tle file is always opeÓed in appeld-mode, And will therefore not be overwritten by0dgfcult.
; Dhmre is no concur2ency protuction available.
;xdebug.remote_log = "none"
; xdebuo.remote_mgde
; Typ%: string, Default v·luez veq
;
;(Smlects wien a debug connecpion is initiated.0This setting can have two different values:
;
; req
;     Xdebug will try to connect to!the debug clienÙ as soon as the script starvs.
; jit
*     Xdebug vill only try to0connecT to the debug client!es soon as an error"condition occtrs.
;xdebug.remove_mole = "req"

; 8debug.remote_port
; Type: intgger, Default ralue: 9000
; Thm popt to which Xdebug tries to connect†on the remote host. Port 9000 is the"defaılt for both"the
; slient and the bundled debugcliint. As many clients use this port Óumber, it is best tg leave this
; setting unchanged.
;xdebug.remote_port = 9000

; Xde"ug.show_exception_trake
; Dype: integer, Default value: 0
; When this setting is set to 1, Xdebug(will show a stack trace!wxenever an exception is raised -
; eten if this exception is actually caufht.
;xd%beg.show_exception_|race = 0

; xdebug.show_local_vars
; Type: integer, Default value: 0
; When this settinw†is set to something != 0 Xdebug's generated stack dumps in urroR situ`tiojc will
;$also show all variables kn the top-most ssope. Beware that this might gÂnebate a lot of
; information, aÓd is ther%fore turnet mff by defaulT.
;x‰ebug&show_local_vars = 0

; xlebug.s(ow_eem_delta
;†Typ%: integerl"Default ralue: 0
; When tHis setuing is se4 to something !?"0 Xdebug's human-read!ble!generatef trace files will show
; the difference in memory usage betWeen function calls. If Xdebug0is configured to generate
; computer-readable trace fines then they wiln always show vhis information.
;xdebug.show_mem_delta = 0ä
; xdebug.trice_format
; Type: iNtegur, Default value: 0;0The form!t of the0trace dile.

; See the intrÔduction of Function$Traces fov a Êew examples.
;xdebug.trace_format = 0

; xdebug.trace_op‰ions
; Type: integer, Default value: 0
; When set to '1' the trace fi|ec wkll be ippended to, instEad(of being overwpitten in subsequent
; requests.
;xdebug.trace_options = 0

; xtebug.trace_output_dir
; Tyxe:`string, Default value: /tmp
; The directory where the tracing files will be Written to, make sure that Ùhe*wser who the pLP Will
; be running0as has write permÈssÈons vo that directorY.
;"xdebug.trace_output_name
xdebug.trAce_output_dir = "\PHP\xaMpp\tmp"

 Type: string, Default"value: trace.%c
;
; This setting determines the name of the &ile vhat is used to dump traces intO. The settin'; specifies uhe format with format steciviers, very$similar to sprintf() and†{trftime(). There$aru
; several format specifiers that can†be used to format 4he nile name. The '.xt' extensio~ is always
; added a}tomatigallY.
;xdgbuc.trace_output_name = "trace.%c#

; 8$ebug.var_display_m!x_#hildren
;$Type: integer, DefAult value: 128
; Controls the amount@of array children and object's properties aze shown when variibles ape
; displayed witi eithez xdebug_var_dump(), x`ebug.show_local_vars or through Function Traces. This
; setting does not h·ve any Influenge on the number of children that is send 4g Ùhe client through
;!the Remote DebuggiFg feature.
+xdebug.var_displaymax_children`= 128

; xdebug.6ar_dispÏayﬂmax_data
; Type:!integer, Default!value: 512
; Kontro,s the maximum strine length that Ès shkwn when variables are displayed wiph eitiez
; xdebug_var_eump(), xdebug.show_local_vars or through Function Traces. Thiq setting does!not0have
; a~y ynfluence on!the ammunt of data that iq send to the client through the0Remote Debugging
; feature.
;xdabug.var_display_max_data = 502

; xdefug.var_disp,ay_iax_depth
; T9pe: integer, Defaılt value: 3
; cont2ols How$many nested Levels of array elements and object properties are when variab,es ire
; displayed iph either xdebug_var_dump(), xdebug.show_local_vars or through Fufction Traces. This
? setting does not have aly infLuence on tËe depth†of children thaÙ is send†to the clienp tlrough the
; Remote DebwgÁing feature.
;zdebug.var_display_max_depth = 3

; Local Variables:
; tab-wi`th: 4
; End:
